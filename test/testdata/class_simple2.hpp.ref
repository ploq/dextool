#ifndef class_simple2.hpp.out
#define class_simple2.hpp.out
class StubSimple : public Simple {
public:
    StubSimple();
    StubSimple(char foo);
    ~StubSimple();

private:
};

#endif // class_simple2.hpp.out
