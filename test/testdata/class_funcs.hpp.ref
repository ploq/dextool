#ifndef class_funcs.hpp.out
#define class_funcs.hpp.out
class StubSimple : public Simple {
public:
    StubSimple();
    StubSimple(char foo);
    ~StubSimple();

private:
};

#endif // class_funcs.hpp.out
